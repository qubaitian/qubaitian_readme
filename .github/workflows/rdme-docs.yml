# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-12-21T02:59:50.918Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

#      - name: Run `docs` command 🚀
#        uses: readmeio/rdme@v8
#        with:
#          rdme: docs ./intro --key=${{ secrets.README_API_KEY }} --version=1.0

      # base operations
      - name: Update base operations 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi ./api/1.yaml --key=${{ secrets.README_API_KEY }} --id=6583dbdbdbfad200769a18f5 --useSpecVersion

      # base operations
      - name: Update base operations 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi ./api/2.yaml --key=${{ secrets.README_API_KEY }} --id=6583e28477c3a1004ecbf189 --useSpecVersion