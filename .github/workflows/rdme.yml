# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-12-21T08:24:07.680Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `v1.0.1`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - v1.0.1

jobs:
  rdme-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

#      - name: Run `docs` command 🚀
#        uses: readmeio/rdme@v8
#        with:
#          rdme: docs ./guides --key=${{ secrets.README_API_KEY }} --version=1.0.1
#
#      - name: Run `changelogs` command 🚀
#        uses: readmeio/rdme@v8
#        with:
#          rdme: changelogs ./changelogs --key=${{ secrets.README_API_KEY }}

      - name: Run `docs` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: docs ./reference --key=${{ secrets.README_API_KEY }} --version=1.0.1

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi ./api/repo-api-tokens.yaml --key=${{ secrets.README_API_KEY }} --id=658a8085ddbf15002d3426cb

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi ./api/quickstart.yaml --key=${{ secrets.README_API_KEY }} --id=658d1b9f74a8660079f3a72e
